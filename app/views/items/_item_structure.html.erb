<div class="row">
  <div class="col s8">
    <!-- primary partial gets displayed here in main div -->
    <%= render partial: "item_data" %>
    <!-- sometimes we want another partial below -->
  </div>
  <div class="col s4">
    <!-- render the sidebar, if it exists -->
    <%= render partial: "similar_items"%>
  </div>
</div>
<div class="row">
  <%= render :partial => "partials/display_base_errors", locals: {object: @item} %>
  <%= render :partial => "item_options" %>
</div>